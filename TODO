# TODO

- Rename address en id dans les tokens & ajouter la properties tokenContract
- Ajout de la fonction cache.setTokensPricesSource(), utile pour des AMM
- Auto Initialisation du cache dans portolfio-api
- Gestion des name services
- Verifier qu'une address a deja passé au moins un TX avant de tout fetch

# DONE

- Fix le problème d'import Job et Fetcher lors de la génération d'un plugin (toujours présent)
- Déplacer les plateformes dans le dossier des plugins correspondant ?
- Ajout d'un module de decimals dans le cache
- Fix les packages utilisant @metaplex-fundation/js (build fail)
- Opti orca-clmm-positions
- Opti raydium-clmm-positions
- Rename cache.getTokenPrices en getAllTokenPrices et créer getTokenPrices
- Fix le "-" dans le nom du fichier d'un fetcher lors de leur génération
- cache getItems(string[])
- "name" pour un compte de lending
- Improve generator
- Revoir les platforms
- Fix parseTypeString function
- Mettre un cache de cache sur TokenPrice
- Create cache server public
- Host caches sur server
- Merge les 2 caches
